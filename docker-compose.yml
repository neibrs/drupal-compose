version: '3'

services:
  drupal:
    image: neibrs/drupal
    container_name: drupal
    environment:
      XDEBUG_CONFIG: remote_host=docker.for.mac.host.internal
    ports:
      - 9104:80
    volumes:
      - ./web:/var/www/html
